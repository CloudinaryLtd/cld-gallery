import { h } from 'preact';
import './Gallery.scss';
import { Carousel } from "../Carousel/Carousel";

const Gallery = () => {
  return (
    <div>
      <Carousel />
    </div>
  );
};

export default Gallery;
