import { h } from 'preact';

import "./Navigator.styles.scss";

const Navigator = props => {
  return (
    <div className="navigator">
      <h1>CLD-GALLERY Navigator COMPONENT</h1>
    </div>
  );
};

export default Navigator;


