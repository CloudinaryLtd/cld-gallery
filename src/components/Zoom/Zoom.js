import { h } from 'preact';

import "./Zoom.styles.scss";

const Zoom = props => {
  return (
    <div className="zoom">
      <h1>CLD-GALLERY Zoom COMPONENT</h1>
    </div>
  );
};

export default Zoom;


